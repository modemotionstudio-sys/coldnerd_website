/**
 * When false, the Free Trial pricing card is hidden on `/pricing` and the home
 * pricing section. Set to `true` to show the trial card again everywhere.
 */
export const SHOW_FREE_TRIAL_PRICING_CARD = false;
